from abc import ABCMeta, abstractmethod
from _collections_abc import Iterable
from abc import ABC

class Deadline_reminder():
    Iterable(ABCMeta)

    def is_due():
        pass
    abstractmethod

class DeadLineReminder(ABC, Iterable):
    def is_due():
        pass
    abstractmethod

